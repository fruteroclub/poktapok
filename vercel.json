{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "if [[ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" && \"$VERCEL_GIT_COMMIT_REF\" != \"dev\" ]]; then exit 0; fi; if git diff --name-only HEAD~1 | grep -qvE '\\.(md|txt)$'; then exit 1; else exit 0; fi"
}
